// is map permanent?

const array = ['heard', 'it', 'in', 'a', 'love', 'song']

const print = word => console.log(word)

array.map(word => word.toUpperCase()).forEach(print)

array.forEach(print)

// NO
